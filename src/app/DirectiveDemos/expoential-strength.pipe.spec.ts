import { ExpoentialStrengthPipe } from './expoential-strength.pipe';

describe('ExpoentialStrengthPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpoentialStrengthPipe();
    expect(pipe).toBeTruthy();
  });
});
